#include <stdio.h>
#include<stdlib.h>
int main()
{
    int i,*a;
    a =(int*)malloc(5*sizeof(int));
    printf("Enter the value = ");
    for (i = 0; i < 5; i++)
    {
        printf("%d ", &b[i]);
    }
    return 0;
}